package event;
import entity.Enemy;
import entity.Entity;
import entity.Player;
import core.Game;
public class Combat {
	public Combat(Game game) {
		while(Entity.getHealth() > 0 && Enemy.getHealth() > 0) {
			
		}
	}
}
